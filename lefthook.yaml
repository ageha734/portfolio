pre-commit:
    parallel: true
    commands:
        lint-staged:
            run: bunx lint-staged
            stage_fixed: true

commit-msg:
    commands:
        commitlint:
            run: bunx commitlint --edit {1}
            tags: commit
